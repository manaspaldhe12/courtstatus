# Copy to .env.local for local dev (never commit the real values here —
# though the anon key is safe to expose in a built frontend, .env.local
# keeps it out of git history for tidiness).
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
